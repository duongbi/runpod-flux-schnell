# Bộ version cho FLUX.1-Kontext-dev.
# - diffusers cần >=0.35 mới có FluxKontextPipeline.
# - diffusers mới đăng ký custom op flash-attn cần torch >=2.5 (torch 2.4.1 gây lỗi
#   infer_schema lúc import) -> Dockerfile đã nâng base image lên torch 2.6.
# - Vẫn chặn transformers 5.x / huggingface_hub 1.x vì chúng làm vỡ import diffusers.
runpod>=1.6.0,<2
diffusers==0.35.1
transformers>=4.51,<4.56
huggingface_hub>=0.30,<1.0
accelerate>=1.0,<2.0
safetensors>=0.4.5
sentencepiece==0.2.0
protobuf>=4.25,<6
Pillow>=10.0.0
